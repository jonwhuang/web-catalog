= form_for (@order_detail), remote: true do |f|
  %p= f.label :quantity
  %p= f.number_field :quantity, :step => 1

  %button.btn.btn-default.in-stock-btn{type: "submit"}
    Add to
    %span.glyphicon.glyphicon-shopping-cart

.flash-order-alerts
  = render 'layouts/order_errors'
